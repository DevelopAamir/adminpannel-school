import 'package:flutter/material.dart';

class Examtake extends StatelessWidget {
  const Examtake({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Take Eaxam'),
        backgroundColor: Color(0xff9CCDDB),
      ),
    );
  }
}
